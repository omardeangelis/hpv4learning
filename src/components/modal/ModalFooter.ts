import styled from "@emotion/styled";
import { Box } from "@mui/system";

export const ModalFooter = styled(Box)({
  marginTop: "auto",
  marginBottom: "24px",
  marginLeft: "auto",
  marginRight: "auto",
  width: "100%",
});
